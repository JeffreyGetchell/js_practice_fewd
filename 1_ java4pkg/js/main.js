// Make it rain! This just happens to a first code project for dims & wts!

var area = function(dims, length, width, height) {
  console.log(dims + " = " + length * width * height + " cubic inches");
}

area("10 x 7 x 4 ", 10, 7, 4);
